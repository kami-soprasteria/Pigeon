package se.soprasteria.automatedtesting.webdriver.weare;


import org.junit.Assert;
import org.junit.Test;
import se.soprasteria.automatedtesting.webdriver.android.AndroidBaseTestCase;

public class WeareHelperTest extends AndroidBaseTestCase {

    @Test
    public void Demo() {

        /*String appTitle = driver.findElement(by).getText();
        String wearTitle = wearDriver.findElement(home.saveJob_Lbl).getText();

        Assert.assertTrue(appTitle.equals(wearTitle), "Fail");*/
    }
}
